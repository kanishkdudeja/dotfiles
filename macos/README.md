# macOS-only files

- `ssh/config`: OrbStack include and Apple `UseKeychain` configuration.
- `tmux/supa.sh`: personal Supabase tmux development session.

These files are intentionally excluded from the Pop!_OS and Omarchy
installers.
