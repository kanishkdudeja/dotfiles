if uname -r |grep -q 'microsoft' ; then
    eval `keychain --quiet --eval --agents ssh id_ed25519`
fi

# Adds a blank line before every prompt. Disabled because Starship already
# manages its own leading blank line (add_newline); enabling both = double gap.
# Uncomment to re-enable if not using Starship.
# _add_newline_before_prompt() {
#   echo ""
# }
#
# if [[ -z "$precmd_functions[(r)_add_newline_before_prompt]" ]]; then
#   add-zsh-hook precmd _add_newline_before_prompt
# fi
